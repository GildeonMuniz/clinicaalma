apiVersion: v1
kind: Service
metadata:
  name: clinicaalma-frontend
  labels:
    app: clinicaalma-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: clinicaalma-frontend
